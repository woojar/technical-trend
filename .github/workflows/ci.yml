name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: uv.lock

      # Include the docs group so the site generator's tests actually run here
      # rather than skipping.
      - run: uv sync --frozen --group docs

      - run: uv run ruff check .

      - run: uv run ruff format --check .

      # The suite makes no network calls, so it is safe to run on every PR.
      - run: uv run pytest
